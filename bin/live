#!/bin/bash

export RUBYOPT="-Ilib $RUBYOPT"

export travis_config=$(base64 --decode <<< "$travis_config_encoded")

cmd="bundle exec je sidekiq -c 25 -r ./lib/travis/live/pusher.rb -q pusher-live"

echo "Running: $cmd"
$cmd
